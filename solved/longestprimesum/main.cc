#include <iostream>
using namespace std;

int main(void) {
    // max primes is as defined as follows...
    // ans = (int)(n / 2).
    unsigned long long int n;
    cin >> n;
    cout << (unsigned long long int)(n / 2) << endl;
    return 0;
}
